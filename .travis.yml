language: java
sudo: true

cache:
  directories:
  - $HOME/.gradle/caches/
  - $HOME/.gradle/wrapper/

jobs:
  include:
  - stage: build vaunt with gradle & generate reports
    script:
    - ./gradlew clean build jacocoRootReport coveralls